<div class="container">
<%= tag.h1('New Post', class: 'h1') %>
<%= form_with model: Post.new do |f| %>
  <%= f.text_field :title, placeholder: 'Title' %>
  <%= f.text_area :text, rows: 20, placeholder: 'Enter the content' %>
  <%= f.submit 'Save' %>
<% end %>
</div>
